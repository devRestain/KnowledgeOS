# Agent Execution Pipeline

- Read `PROJECT_STATE.md` before selecting or promoting a slice.
- Resolve conflicts in `OBSIDIAN_VAULT_BLUEPRINT.md`, `blueprint/blueprint.yaml`, then `OBSIDIAN_VAULT_WHITEPAPER.md` order.
- Implement acceptance-gated slices instead of bulk-scaffolding declared commands.
- Keep the pipeline order `validator -> artifacts -> note engine -> portable Vault -> local transactions -> Git/mobile -> proposal-only LLM -> projection -> retrieval`.
- Keep MacBook-first order `S13C -> S13D -> S14 -> S11/S12 -> S10 mobile` after completed S13A/S13B.
- Enter S13C only after S13B create-only and guarded-formatting evidence remains valid.
- Require S13C to reject unsafe assets, guard hash-bound replacement or movement, preserve finalize/archive identity and links, and trace binary provenance.
- Keep S13D runtime recovery, S14 bridge/Git publish, S15/S16 proposal lanes, and S17-S19 projection/retrieval lanes separate.
- Treat optional vector, LaunchAgent, remote, provider, and thin-client work as inactive opt-ins.
- Record each slice as current, next, blocked, deferred, or history in `PROJECT_STATE.md` only.
