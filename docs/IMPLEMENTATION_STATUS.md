# Agent Status Index

- Read `PROJECT_STATE.md` for the sole machine-state authority: checkpoint, current goal, next goal, blockers, evidence, decisions, scopes, and handoff.
- Treat this file as a compact human-readable index, not a competing state source; validate capability claims against the live worktree, manifests, source, tests, runtime artifacts, and recorded evidence.
- C01 through C43 are complete at their accepted contract boundaries. E02 host verification and the E03 provider-free current-user LaunchAgent boundary are complete; remote provider routes and unattended provider activation remain deferred.
- The local embedding policy selects `qwen3-embedding:8b-q4_K_M` as the default and `embeddinggemma:300m-qat-q8_0` as the emergency resource fallback. The separate Qwen candidate gate passed, while durable C34 pointer materialization remains blocked until an eligible canonical corpus exists.
- F01-F06 and the Vaultctl closure are complete. Obsidian Core owns daily creation; Notebook Navigator owns weekly/monthly creation and opening; Templater renders bounded period fields; `vaultctl note validate` validates the resulting note; `vaultctl obsidian status` is the only public official-CLI adapter and remains status-only.
- P01 and P02-P12 setting-state contracts are complete at their declared static/semantic boundaries. The Mac allowlist now contains exactly eleven community plugins, including `knowledgeos-thin-client`; the mobile community-plugin baseline remains empty. A profile audit is not device execution evidence.
- H01/H03 completed the authorized MacBook artifact, period-note, Home, plugin-setting, and device-evidence slices while preserving separate app-connection, mobile, Git-network, and external-service boundaries.
- C41-C43 and E05 provide a removable desktop presentation client, an authenticated `127.0.0.1` broker, bounded citation/diff/review presentation, safety gates, and rollback contracts. The plugin never owns retrieval, provider, Vault-write, Git, or canonical-apply authority.
- The authorized E05 live integration installed and enabled the thin client, deployed the persistent loopback broker, built the deterministic projection used for digest rechecks, executed one bounded local `gemma4:12b` route through Ollama with a 600-second limit, and verified one Obsidian device request. The provider response remained proposal-only; the device request returned a bounded no-eligible-evidence conflict without mutation. One separate provider-free frozen proposal was reviewed, approved, applied, and receipt-verified through C19.
- The current canonical control checks pass: source checksum, foundation verification, Blueprint schema/semantic validation, generated-artifact zero-diff, container source/verification, 427 tests, and sequential Ruff lint. Control and Vault whitespace checks pass without overwriting unrelated dirty Vault paths.
- Keep the normal user journey GUI-first and human-controlled. Use `vaultctl` for validation, retrieval, proposal, approval, apply, receipts, diagnostics, and bounded local routes; treat external service, deployment, device, Git, and canonical-apply evidence as separate classes.
