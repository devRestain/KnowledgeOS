# Agent Status Index

- Read `PROJECT_STATE.md` for the authoritative status, checkpoint, current goal, next goal, blocker, evidence, and handoff.
- Treat this file as a compact index, not a competing machine-state source; use the live worktree, manifests, source, tests, and recorded evidence for capability claims.
- C01 through C36, E01 through E03, and the Mac deployment baseline through D07 are complete at their recorded boundaries. C37 is complete for the finalized plugin-profile/model-identity contract and generated-artifact regeneration; C38 through C43 are registered as the ordered follow-up implementation lane.
- E02 host-native verification is complete, but model or learned-retrieval promotion remains gated by measured quality, privacy, citation, staleness, latency, and resource evidence.
- E03 is the active provider-free current-user LaunchAgent boundary: local bridge and recovery wakes remain separate from provider queue consumption, canonical Vault mutation, remote lanes, and device effects.
- E04 is the current planned remote or unattended lane; P01 is the next planned read-only core and community-plugin setting-state registry.
- P01 may reuse the observed ten-plugin Mac profile and core baseline, but setting, runtime, and device verification remain unrun until that slice starts.
- F01 through F06 are registered as a separate GUI-first period-note and `vaultctl` boundary refactor: F01 removes the period writer, F02 migrates weekly/monthly rendering to bounded Templater fixtures, F03 aligns Notebook Navigator paths and titles, F04 makes Note Toolbar the primary user surface, F05 adds only a bounded `vaultctl obsidian status` adapter, and F06 closes cross-class evidence. F does not authorize Obsidian UI, plugin, device, remote, LaunchAgent, Git, or live-provider effects.
- F is a target contract rather than completed implementation: the legacy period parser/workflow/command entries and current Notebook Navigator patterns remain live until their corresponding F acceptance slices pass; static planning text must not be promoted to runtime, artifact, deployment, or device evidence.
- C38 locks the `gemma4:12b` identity through an evidence envelope; C39 gates local embedding/index promotion; C40 promotes brokered proposal-only routes; C41 defines the removable Obsidian thin client; C42 closes privacy/citation/quality observability; and C43 defines operations and rollback. None of these labels authorize live Ollama, plugin/device, remote, LaunchAgent, or Git effects.
- Keep history, obsolete counts, superseded snapshots, and resolved rationale out of this index; retain active policy boundaries in `PROJECT_STATE.md` and detailed execution order in `IMPLEMENTATION_PLAN.md`.
